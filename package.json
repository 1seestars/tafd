{
  "name": "tafd",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "client": "yarn workspace client start",
    "server": "yarn workspace server dev",
    "start": "concurrently --kill-others-on-fail \"yarn server\"  \"yarn client\"",
    "fix:prettier": "./node_modules/.bin/prettier --write \"**/*.ts\""
  },
  "devDependencies": {
    "concurrently": "^5.3.0",
    "prettier": "^2.2.1"
  }
}
